export interface Tokens{
    access_token: string;
    refresh_token: string;
    "as:client_id": string;
    ".issued": string;
    ".expires": string;
    expires_in: number;
    token_type: string;
}